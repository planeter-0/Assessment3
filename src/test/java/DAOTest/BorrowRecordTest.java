package DAOTest;

import data_access.BorrowRecordDAO;
import entity.BorrowRecord;
import java.sql.Date;

public class BorrowRecordTest {
    public static void main(String[] args) throws Exception {
        //增 通过
//        BorrowRecord record = new BorrowRecord(2,8,new Date(System.currentTimeMillis()));
//        BorrowRecordDAO recordDAO = new BorrowRecordDAO(record);
//        recordDAO.create();

        //删 通过
//        BorrowRecord record = new BorrowRecord(8);
//        BorrowRecordDAO recordDAO = new BorrowRecordDAO(record);
//        recordDAO.delete();

        //改 通过
//        BorrowRecord record = new BorrowRecord(8);
//        BorrowRecordDAO recordDAO = new BorrowRecordDAO(record);
//        recordDAO.update(3,7,new Date(System.currentTimeMillis()),true);
        //查 通过
//        BorrowRecord record = new BorrowRecord();
//        BorrowRecordDAO recordDAO = new BorrowRecordDAO(record);
//        BorrowRecord selectedRecord =  recordDAO.queryById(7);
//        System.out.println(selectedRecord);
    }
}
